{% extends 'TicmeBackBundle::layout.html.twig' %}

{% block content %}
    <div class="row">
        <div class="col-lg-12">
            <h1>Commandes {{ ordering.reference }}</h1>
        </div>
        <div class="col-lg-6">
            {{ ordering.products }}
            {{ ordering.validated }}
            {{ ordering.createdAt }}
        </div>
    </div>
    <br />
    <div class="row">
        <div class="col-lg-12">
            <a href="{{ path('ticme_back_order_list') }}" class="btn btn-default"><i class="fa fa-mail-reply"></i> Revenir à la liste des commandes</a>
            <a href="{{ path('ticme_back_order_edit', {'id': ordering.id}) }}" class="btn btn-info"><i class="fa fa-edit"></i> Editer</a>
        </div>
    </div>
{% endblock %}